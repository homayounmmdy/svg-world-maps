---
title: São Tomé and Príncipe Map
description: Interactive SVG map of São Tomé and Príncipe with all subdivisions. Features tooltips, click handling, and hover colors inspired by the national flag.
sidebar_position: 60
sidebar_label: São Tomé and Príncipe
---

import SoTomAndPrncipeMap from '../../components/maps/st';
import Admonition from '@theme/Admonition';

# 🇸🇹 São Tomé and Príncipe Interactive Map

São Tomé and Príncipe is a small island nation in the Gulf of Guinea, known for its lush rainforests, pristine beaches, and historic Portuguese colonial architecture.

## 🌍 Overview

São Tomé and Príncipe is a small island nation in the Gulf of Guinea, known for its lush rainforests, pristine beaches, and historic Portuguese colonial architecture.

### 🎨 Design & Flag Inspiration
The visual identity of this map pack is inspired by the **National Flag of São Tomé and Príncipe**.
* **Hover State:** We utilize a vibrant translucent color (`rgba(0, 158, 48, 0.35)`) to represent the national identity and heritage.
* **Borders:** Rendered in Deep Navy (`#1e293b`) to ensure maximum contrast and a modern, premium aesthetic.

---

## 🗺️ Interactive Demo

Hover over the Districts to see the highlight and tooltips. **Click any district** to see how easily you can capture the region's data code.

<SoTomAndPrncipeMap />

<Admonition type="info" title="How it works">
Every `<path>` element in the generated SVG automatically receives `data-code` and `data-name`. You can listen for clicks on the parent container to instantly build drill-down experiences, filtering, or regional detail views.
</Admonition>

---

## 💻 How to Render It

Implementing the **São Tomé and Príncipe** map in your project takes just a few lines of code. Since every map is generated as a plain SVG string, the exact same approach works in **React**, **Vue**, **Angular**, **Svelte**, and **vanilla JavaScript**.

<Admonition type="tip" title="One pattern, every framework">
Generate the SVG with <code>createMap()</code>, inject it into a container, and listen for clicks using the automatic <code>data-code</code> and <code>data-name</code> attributes.
</Admonition>

👉 **[See the full rendering guide for your framework →](/docs/how-to-render)**